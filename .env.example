# Environment variables for portfolio website

# API endpoint for backend services (set after terraform deployment)
VITE_API_ENDPOINT=https://your-api-gateway-url.execute-api.us-east-1.amazonaws.com

# Demo mode configuration
VITE_DEMO_MODE=true

# AWS region (for reference)
AWS_REGION=us-east-1

# Project name (matches terraform configuration)
PROJECT_NAME=ecommerce-analytics-demo
